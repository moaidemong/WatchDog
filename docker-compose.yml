version: "3.9"
services:
  api:
    build: .
    command: uvicorn app.api.server:app --host 0.0.0.0 --port 8000
    env_file:
      - .env.example
    volumes:
      - ./:/workspace
    working_dir: /workspace
    ports:
      - "8000:8000"
