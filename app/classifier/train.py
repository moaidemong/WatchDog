from __future__ import annotations

from pathlib import Path


def train_classifier(dataset_path: str | Path) -> None:
    # Placeholder intentionally simple. Replace with sklearn baseline first.
    print(f"[TODO] train classifier using dataset: {dataset_path}")
